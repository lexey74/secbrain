# 🎭 Безопасный скрапинг комментариев через Playwright

## 📋 Обзор

SecBrain поддерживает **два метода** получения комментариев:

1. **Gallery-dl** (быстро, но может не работать)
2. **Playwright** (медленно, но максимально безопасно) ✅ Рекомендуется

## 🔐 Почему Playwright безопаснее?

### Gallery-dl (обычный способ):
- ❌ Прямые API запросы
- ❌ Легко детектируется Instagram
- ❌ Может привести к бану IP

### Playwright (безопасный способ):
- ✅ Запускает **настоящий браузер** (Chromium)
- ✅ Эмулирует человеческое поведение (скроллинг, паузы)
- ✅ Перехватывает GraphQL запросы "на лету"
- ✅ Полный fingerprint браузера (Canvas, WebGL, AudioContext)
- ✅ Скрывает факт автоматизации (`--disable-blink-features=AutomationControlled`)

## 📦 Установка

```bash
# 1. Установите Playwright
pip install playwright

# 2. Установите браузер Chromium
playwright install chromium

# 3. Проверка
python -c "from playwright.sync_api import sync_playwright; print('OK')"
```

## 🔑 Настройка Cookies

### Вариант 1: Расширение для Chrome (рекомендуется)

1. Установите расширение **"EditThisCookie"** или **"Get cookies.txt"**
2. Войдите в Instagram через браузер
3. Откройте расширение → Export → JSON формат
4. Сохраните в файл `instagram_cookies.json` в корне проекта

### Вариант 2: Ручной вход (первый запуск)

1. Запустите с `headless=False` (видимое окно)
2. Скрипт откроет браузер
3. Войдите вручную в Instagram
4. Cookies сохранятся автоматически

## 🚀 Использование

### Через download.py

```bash
$ python src/download.py

🎭 Использовать безопасный скрапер комментариев (Playwright)? [y/n]: y
✅ Режим безопасного скрапинга включен

Instagram URL: https://www.instagram.com/p/ABC123/

🎭 Запуск безопасного браузера...
🔗 Переход на: https://www.instagram.com/p/ABC123/
   ✅ Cookies загружены
   🤖 Эмуляция поведения пользователя (15 сек)...
   ⬇️  Нажата кнопка загрузки комментариев
   ✅ Эмуляция завершена (скроллов: 8)
   📊 Перехвачено пакетов данных: 3
   💬 Извлечено комментариев: 42
   ✅ Сохранены comments.md (42 комментариев)
```

### Программно

```python
from modules.safe_comments import SafeCommentsScraper

scraper = SafeCommentsScraper(
    cookies_file="instagram_cookies.json",
    headless=True  # False = видимое окно
)

comments = scraper.scrape_comments(
    post_url="https://www.instagram.com/p/ABC123/",
    scroll_duration=20  # Секунд скроллинга (больше = больше комментариев)
)

for comment in comments:
    print(f"{comment['user']}: {comment['text']}")
    print(f"Likes: {comment['likes']}")
```

## ⚙️ Настройка

### В config.json

```json
{
  "cookies_file": "instagram_cookies.json",
  "headless_browser": true,
  "save_raw_comments": false,
  "scroll_duration": 15
}
```

### Параметры:

- `headless_browser`: `true` = фоновый режим, `false` = видимое окно
- `save_raw_comments`: Сохранять сырые GraphQL данные для отладки
- `scroll_duration`: Секунд скроллинга (10-30 рекомендуется)

## 🧠 Как это работает?

### 1. Запуск браузера с антидетект параметрами

```python
browser = p.chromium.launch(
    headless=True,
    args=["--disable-blink-features=AutomationControlled"]
)
```

### 2. Перехват GraphQL запросов

```python
def handle_response(response):
    if "graphql/query" in response.url:
        data = response.json()
        # Извлекаем комментарии из ответа
```

### 3. Эмуляция человека

- ✅ Случайный скроллинг (300-700px)
- ✅ Случайные паузы (1.5-3.5 секунды)
- ✅ Нажатие "Load more comments"
- ✅ PageDown каждые 5 скроллов

### 4. Парсинг комментариев

```python
comments = [
    {
        'id': '12345',
        'user': 'username',
        'text': 'Comment text',
        'likes': 42,
        'created_at': 1234567890
    },
    ...
]
```

## 🎯 Примеры

### Пример 1: Скрапинг с видимым окном

```bash
# Для отладки/первого запуска
python -c "
from modules.safe_comments import SafeCommentsScraper

scraper = SafeCommentsScraper(headless=False)  # Видимое окно
comments = scraper.scrape_comments(
    'https://www.instagram.com/p/ABC123/',
    scroll_duration=10
)
print(f'Получено {len(comments)} комментариев')
"
```

### Пример 2: Batch скрапинг

```python
urls = [
    "https://www.instagram.com/p/ABC123/",
    "https://www.instagram.com/p/DEF456/",
    "https://www.instagram.com/reel/GHI789/"
]

scraper = SafeCommentsScraper(headless=True)

for url in urls:
    comments = scraper.scrape_comments(url, scroll_duration=15)
    print(f"{url}: {len(comments)} комментариев")
    time.sleep(random.randint(30, 60))  # Пауза между постами
```

### Пример 3: Сохранение сырых данных

```python
scraper = SafeCommentsScraper()
comments = scraper.scrape_comments(url)

# Сохраняем сырые GraphQL ответы для анализа
scraper.save_raw_data("raw_comments.json")
```

## 🐛 Troubleshooting

### Playwright не установлен

```bash
pip install playwright
playwright install chromium
```

### Cookies не работают

1. Убедитесь, что cookies в JSON формате
2. Проверьте, не истекли ли cookies (войдите заново)
3. Используйте `headless=False` для ручного входа

### Браузер не запускается

```bash
# Linux: установите зависимости
playwright install-deps chromium

# Или используйте headless=True
```

### Не находит комментарии

1. Увеличьте `scroll_duration` (20-30 секунд)
2. Проверьте, что пост публичный
3. Убедитесь, что у поста есть комментарии

### Ошибка: "AutomationControlled"

Это нормально, если cookies работают. Флаг скрывает автоматизацию.

## 📊 Производительность

**Один пост:**
- Запуск браузера: ~3-5 сек
- Загрузка страницы: ~3-5 сек
- Скроллинг (15 сек): ~15 сек
- Парсинг: ~1 сек
- **Итого: ~25-30 секунд**

**10 постов:**
- Без пауз: ~4-5 минут
- С паузами (30-60 сек): ~10-15 минут

## 🔒 Безопасность

### Что делает скрипт безопасным:

1. **Реальный браузер**: Instagram видит настоящий Chrome
2. **Эмуляция человека**: Случайные паузы, скроллинг
3. **Нет прямых API**: Не делает "левых" запросов
4. **Fingerprint**: Полный отпечаток браузера
5. **Cookies**: Используются ваши легитимные cookies

### Рекомендации:

- ✅ Делайте паузы между постами (30-60 секунд)
- ✅ Не скрапьте больше 50-100 постов за сессию
- ✅ Используйте свои cookies (не чужие аккаунты)
- ✅ Скрапьте только публичные посты
- ⚠️ Не запускайте 24/7

## 🆚 Сравнение методов

| Параметр | Gallery-dl | Playwright |
|----------|-----------|------------|
| Скорость | ⚡⚡⚡ (~5 сек) | ⚡ (~30 сек) |
| Безопасность | ⚠️ Низкая | ✅ Высокая |
| Комментарии | ❌ Не всегда | ✅ Да |
| Cookies | ✅ Нужны | ✅ Нужны |
| Ресурсы CPU | 💚 Низкие | 🔶 Средние |
| Риск бана | 🔴 Высокий | 🟢 Низкий |

## 📝 FAQ

**Q: Можно ли запускать на VPS без GUI?**  
A: Да, используйте `headless=True`. Но нужно вручную получить cookies на локальной машине.

**Q: Сколько комментариев можно получить?**  
A: Зависит от `scroll_duration`. 15 секунд = ~20-50 комментариев, 30 секунд = ~50-100.

**Q: Почему не получаю все комментарии?**  
A: Instagram подгружает комментарии порциями. Увеличьте `scroll_duration` или используйте несколько запусков.

**Q: Можно ли скрапить приватные аккаунты?**  
A: Только если вы подписаны. Используйте cookies от своего аккаунта.

**Q: Банит ли Instagram за это?**  
A: Риск минимальный, если делаете паузы и не злоупотребляете.

---

**Создано:** 2026-01-10  
**Версия:** 1.0  
**Автор:** @lexey74
