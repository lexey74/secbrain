# === SecBrain General Configuration ===
# Скопируйте этот файл в .env и заполните значения
# --- Telegram Bot Configuration ---
# Telegram Bot Token (получить от @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Разрешённые пользователи (через запятую, пусто = все)
# TELEGRAM_ALLOWED_USERS=123456789,987654321

# --- Content Processing Configuration ---
# Путь к папке downloads (по умолчанию: downloads)
DOWNLOADS_DIR=downloads

# Whisper (STT) settings
# WHISPER_MODEL=small
# WHISPER_THREADS=16

# === MCP Server Configuration ===
# --- Auth settings ---
# Секретный ключ для генерации JWT токенов. Должен быть сложной строкой
# Обязательно для работы сервера!
# Например, можно сгенерировать openssl rand -hex 32
MCP_JWT_SECRET="your_strong_jwt_secret_here"

# Путь к файлу с api-ключами (по умолчанию: auth.json)
# AUTH_FILE=auth.json

# ID пользователя по умолчанию для запросов без аутентификации (в режиме разработки)
# DEFAULT_MCP_USER=1

# --- Network Security ---
# Список разрешенных хостов для подключения к MCP серверу (через запятую)
# Пример: 'localhost:*,192.168.1.10:*'
# FASTMCP_ALLOW_HOSTS='*:*'

# Список разрешенных origins (для CORS, через запятую)
# Пример: 'http://localhost:3000,https://my-app.com'
# FASTMCP_ALLOW_ORIGINS='*'
